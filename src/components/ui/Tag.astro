---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-full border border-border px-3 py-1 text-xs font-medium text-ink-muted',
    className,
  ]}
>
  <slot />
</span>
